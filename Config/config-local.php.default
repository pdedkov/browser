<?php
// глобальные настройки конфигуратора
Configure::write('Browser.Curl.Configurator.disabled', ['ip', 'proxy']);

Configure::write('Browser.Curl.Configurator.proxy', []);

Configure::write('Browser.Curl.Configurator.ip', []);

// cache
Configure::write('Browser.cache', false);

// Драйвер доступа к кэшу
Configure::write('Browser.Db.driver', 'Ssdb'); // Redis, Dummy, Ssdb